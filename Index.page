Title="Bluetooth Manager"
---
<?php

$btmName = 'bluetooth-manager';
$pairedDevices = [];

exec("bluetoothctl devices", $pairedDevices);

?>

<style>
    .btm-container {
        display: flex;
        flex-direction: column;
    }

        .btm-container btm-row {
            flex: 1;
        }

        .btm-container btm-colum {
        }

        .btm-container .btm-title {
            font-weight: bold;
            font-size: 1.2em;
            display: block;
        }

        .btm-container ul {
            list-style-type: none;
            padding: 0px;
            margin: 0px;
        }

            .btm-container ul li {
                display: flex;
                justify-content: start;
                align-items: center;
            }

                .btm-container ul li input, .btm-container ul li span {
                    margin: 3px 3px 0px 5px;
                }

                .btm-container ul li input {
                    
                }

                .btm-container ul li span {
                    
                }
</style>

<div class="btm-container">
    <div class="btm-row">
        <div class="btm-column">
            <span class="btm-title">Paired Devices</span>
            <ul>
                <?php
                foreach ($pairedDevices as $pairedDevice) {
                    $device = explode(" ", $pairedDevice);
                    $deviceId = $device[1];
                    $deviceName = join(" ", array_slice($device, 2));

                    echo '<li><input type="radio" name="paired" value="' . $deviceId . '" /><span>' . $deviceName . ' (' . $deviceId . ')' . '</span></li>';
                }
                ?>
            </ul>
        </div>
    </div>
    <div class="btm-row">
        <button id="disconnect" type="button" onclick="disconnect();" style="display:none;">Disconnect</button>
    </div>
    <div class="btm-output"></div>
</div>

<script>
    function connect() {
        let selectedDeviceId = $('input[type="radio"][name="paired"]:checked').val();
        let url = '/plugins/<?= $btmName ?>/include/Functions.php'
        let data =  { 
            id: selectedDeviceId,
            method: 'sconnect'
        };

        $('div.spinner').show();

        $.post(url, data, (response) => {
            $('div.spinner').hide();

            alert(`action performed successfully: ${response}`);
        });
    }

    function disconnect() {
        let selectedDeviceId = $('input[type="radio"][name="paired"]:checked').val();
        let url = '/plugins/<?= $btmName ?>/include/Functions.php'
        let data =  { 
            id: selectedDeviceId,
            method: 'disconnect'
        };

        $('div.spinner').show();

        $.post(url, data, (response) => {
            $(`input[value="${response}"]`).parent().remove();

            $('div.spinner').hide();
        });
    }

    $(function() {
        $('input[type="radio"]').on('click', (event) => {
            let radio = $(event.currentTarget);

            if(radio.hasClass('checked')) {
                $('#disconnect').hide();

                radio
                    .prop('checked', false)
                    .removeClass('checked');
            }
            else {
                $('#disconnect').show();

                radio
                    .prop('checked', true)
                    .addClass('checked');
            }
        });
    });
</script>